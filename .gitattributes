# 自动检测文本文件，提交时统一转换为 LF（跨平台推荐）
* text=auto

# 强制特定文件类型使用 LF（如代码、配置文件）
*.c   text eol=lf
*.h   text eol=lf
*.s   text eol=lf
*.json text eol=lf
*.yml  text eol=lf
*.md   text eol=lf

# 二进制文件（如图片、库文件）不转换
*.png  binary
*.jpg  binary
*.a    binary
*.lib  binary